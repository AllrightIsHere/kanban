import { remove, removeValidator } from "./remove";
import { update, updateValidator } from "./update";
import { move, moveValidator } from "./move";

export const CardController = {
    remove,
    removeValidator,
    update,
    updateValidator,
    move,
    moveValidator,
};
